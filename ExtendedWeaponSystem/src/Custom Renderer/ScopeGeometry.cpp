#include "Custom Renderer/ScopeGeometry.h"

BSGeometry* CreateScreenQuadShape(float a1, float a2, float a3, float a4) {
	using func_t = decltype(&CreateScreenQuadShape);
	REL::Relocation<func_t> func{ REL::ID(1246233) };
	return func(a1, a2, a3, a4);
}
